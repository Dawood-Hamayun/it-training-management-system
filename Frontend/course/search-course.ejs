<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View</title>
</head>
<body>

    <h1>Search here</h1>
    <form action = "/course/search">
        <input type ="search" name = "name" placeholder = "Search Name">
        <input type ="search" name = "batch" placeholder = "Search batch">
        <input type="search" name = "year" placeholder="Search Year"/>
        <input type = "submit" value ="Search"> <br>
    </form>

    <h1>Course List</h1>

    <table border ='1' width = '100%'>
        <tr>
            <th>Course ID</th>
            <th>Name</th>
            <th>batch</th>
            <th>Year</th>
        </tr>
        <% if(course.length > 0) { %>
            <% for (var i = 0; i<course.length; i++) {%>
                <tr>
                    <td><%- course[i].course_id  %> </td>
                    <td><%- course[i].name  %> </td>
                    <td><%- course[i].batch%> </td>
                    <td><%- course[i].year  %> </td>
                </tr>
            
            <% } %> 
        
        <% } else{ %>
            <tr>
                <td>Data Not Found.</td>
            </tr>
        <% } %> 
    </table>
    
</body>
</html>